gate: plan_check
verdict: GO_WITH_FIXES
by: fable-reviewer
recorded_at: '2026-07-30T02:33:01.264Z'
notes: >-
  Reviewer hand-traced every test against the current implementation and empirically simulated old
  and new writers against the plan exact fixtures on Node v26, baseline confirmed 77/0. All three
  must-fixes are plan-text and test-fixture level, no design change required in the reviewer view —
  but the shaper subsequently proved by simulation that MF1 repair as suggested fails under BOTH
  writers, which escalates A1 into a spec defect. See the deviation recorded immediately after this
  gate.
must_fix:
  - >-
    MF1: Task 2 R-2 test is provably GREEN before its fix — the note text places ## Result mid-line
    so the old first-occurrence replace still finds the real heading. Confirmed by simulation.
    Violates the spec No-Go against fixing a defect before its failing test exists.
  - >-
    MF2: Task 3 Step 2 predicts an unreachable failure — before wiring, house adr hits the
    unknown-command path and exits 2, so the first assertion fails 2!==1 and node:test aborts before
    the deepEqual the plan calls the meaningful failure.
  - >-
    MF3: Task 4 Step 2 predicts the wrong red reason — unit 01 exists, so the lookup succeeds and
    the current trailing else throws unknown unit action; no such unit never appears in that run.
advisory:
  - >-
    A1: the named limit is presented as the only residual hole and is not — a multi-line note
    containing any line starting with ## truncates the computed Heartbeats end for subsequent beats,
    landing the next heartbeat inside the previous note. Same class R-2 exists to fix.
  - >-
    A2: --title flows unescaped into the H1 at slices.js:222, so a title containing a newline plus a
    heading creates a fake first heading. Not a spec violation; a newline guard closes it.
  - 'A3: findSection red-run is a TypeError not a SyntaxError.'
  - 'A4: Step 6 uses fs.globSync (Node >= 22) while the tech-stack line says Node >= 18.'
  - >-
    A5: R-4 missing-heading refusal is tested for finalize only; heartbeat identical path
    unexercised.
lenses:
  arch_fit: >-
    findSection genuinely follows the derive.js:84-112 precedent rather than citing it — positions
    not replacement, missing heading named and refused. adrCmd mirrors prCmd; owned-event pattern
    matches slice.pr_set.
  spec_coverage: >-
    all seven requirements land on a task, none orphaned, no task does unrequested work. Only gap is
    A5.
  risk_sequencing: >-
    counts 77 to 85 arithmetically right, 77 baseline verified by running the suite; no boundary
    regresses; the enum step is load-bearing and correctly sequenced before adrCmd because
    appendEvent validates against event_types.
  testability: >-
    findSection arithmetic verified against every fixture including the adjacent-heading and
    last-section edges; no .stderr references remain; every symbol already imported; Task 4 brace
    deletion structurally valid; Task 3 Step 7 empty-diff prediction is STRONGER than hedged since
    the includes guard means writeYaml is never called. But MF1/MF2/MF3.
  simpler_path: >-
    none that satisfies the spec — two anchored regexes leave R-3 live and fail R-4; deleting adrs
    costs eight hand-edited manifests.
